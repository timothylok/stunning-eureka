{
  "name": "timothylok/stunning-eureka",
  "description": "Open-source GitHub repository at https://github.com/timothylok/stunning-eureka.",
  "evidence": [
    "Repo stats: 23 files. Extensions: .ts:13, .json:6, .md:2, .html:1. tests: no CI workflows: no LICENSE: no Dockerfile: no lockfile: yes",
    "File tree:\n.gitignore (35b)\nCLAUDE.md (9858b)\nmodels.json (840b)\npackage-lock.json (16689b)\npackage.json (360b)\nrubrics/default.json (642b)\nSESSIONS.md (1671b)\nsrc/compare.ts (3000b)\nsrc/engine.ts (1570b)\nsrc/evaluate.ts (1781b)\nsrc/index.ts (1196b)\nsrc/prompt.ts (1133b)\nsrc/providers/anthropic.ts (2025b)\nsrc/providers/ollama.ts (1488b)\nsrc/providers/openai.ts (1903b)\nsrc/providers/provider.ts (265b)\nsrc/providers/registry.ts (723b)\nsrc/report.ts (1827b)\nsrc/server.ts (3316b)\nsrc/types.ts (1133b)\ntargets/example.json (1040b)\ntsconfig.json (227b)\nweb/index.html (9764b)",
    "Config/manifest package.json:\n{\n  \"name\": \"ai-tools-evaluator\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": {\n    \"evaluate\": \"tsx src/index.ts\",\n    \"compare\": \"tsx src/compare.ts\",\n    \"web\": \"tsx src/server.ts\",\n    \"typecheck\": \"tsc --noEmit\"\n  },\n  \"devDependencies\": {\n    \"@types/node\": \"^26.1.0\",\n    \"tsx\": \"^4.23.0\",\n    \"typescript\": \"^6.0.3\"\n  }\n}\n",
    "Source file src/server.ts:\nimport { existsSync, readFileSync, readdirSync } from \"node:fs\";\nimport { createServer, type IncomingMessage, type ServerResponse } from \"node:http\";\nimport { join } from \"node:path\";\nimport { readJson, runEvaluation } from \"./evaluate.js\";\nimport type { EvaluationRun, ModelsFile } from \"./types.js\";\n\nif (existsSync(\".env\")) {\n  process.loadEnvFile(\".env\");\n}\n\nconst PORT = Number(process.env.PORT ?? 3000);\n\nconst server = createServer((req, res) => {\n  handle(req, res).catch((err: unknown) => {\n    sendJson(res, 500, { error: err instanceof Error ? err.message : String(err) });\n  });\n});\n\nasync function handle(req: IncomingMessage, res: ServerResponse): Promise<void> {\n  const url = new URL(req.url ?? \"/\", `http://localhost:${PORT}`);\n\n  if (req.method === \"GET\" && url.pathname === \"/\") {\n    res.writeHead(200, { \"content-type\": \"text/html; charset=utf-8\" });\n    res.end(readFileSync(\"web/index.html\"));\n    return;\n  }\n\n  if (req.method === \"GET\" && url.pathname === \"/api/config\") {\n    const modelsFile = readJson<ModelsFile>(\"models.json\");\n    sendJson(res, 200, {\n      default: modelsFile.default,\n      models: Object.entries(modelsFile.models).map(([id, m]) => ({\n        id,\n  \n…[truncated]",
    "Source file src/index.ts:\nimport { existsSync } from \"node:fs\";\nimport { parseArgs } from \"node:util\";\nimport { readJson, runEvaluation } from \"./evaluate.js\";\nimport type { ModelsFile, Target } from \"./types.js\";\n\nif (existsSync(\".env\")) {\n  process.loadEnvFile(\".env\");\n}\n\nasync function main(): Promise<void> {\n  const { values } = parseArgs({\n    options: {\n      target: { type: \"string\", default: \"targets/example.json\" },\n      rubric: { type: \"string\", default: \"rubrics/default.json\" },\n      model: { type: \"string\" },\n    },\n  });\n\n  const modelId = values.model ?? readJson<ModelsFile>(\"models.json\").default;\n  const targetName = readJson<Target>(values.target).name;\n  console.log(`Evaluating \"${targetName}\" with ${modelId}...`);\n\n  const { run, jsonPath, mdPath } = await runEvaluation(values.target, values.rubric, modelId);\n\n  console.log(`Overall: ${run.result.overall_rating}`);\n  for (const [id, score] of Object.entries(run.result.scores)) {\n    console.log(`  ${id}: ${score}`);\n  }\n  console.log(`Report saved: ${jsonPath}, ${mdPath} (${run.latency_ms} ms)`);\n}\n\nmain().catch((err: unknown) => {\n  console.error(`Error: ${err instanceof Error ? err.message : String(err)}`);\n  process.exit(1);\n});\n",
    "Source file src/compare.ts:\nimport { readdirSync, readFileSync, writeFileSync } from \"node:fs\";\nimport { join } from \"node:path\";\nimport { parseArgs } from \"node:util\";\nimport type { EvaluationRun } from \"./types.js\";\n\nconst REPORTS_DIR = \"reports\";\n\nfunction main(): void {\n  const { values } = parseArgs({\n    options: { target: { type: \"string\" } },\n  });\n\n  const runs = loadRuns();\n  if (runs.length === 0) {\n    throw new Error(`No reports found in ${REPORTS_DIR}\\\\. Run an evaluation first.`);\n  }\n\n  const targets = [...new Set(runs.map((r) => r.target))];\n  const target = values.target ?? (targets.length === 1 ? targets[0] : undefined);\n  if (!target) {\n    throw new Error(`Multiple targets found — pass --target. Available: ${targets.join(\", \")}`);\n  }\n\n  // Latest run per model for the chosen target.\n  const latestByModel = new Map<string, EvaluationRun>();\n  for (const run of runs.filter((r) => r.target === target)) {\n    const existing = latestByModel.get(run.model_provider_id);\n    if (!existing || run.started_at > existing.started_at) {\n      latestByModel.set(run.model_provider_id, run);\n    }\n  }\n  if (latestByModel.size < 2) {\n    throw new Error(\n      `Need reports from at least 2 models for \"${t\n…[truncated]",
    "Security scan — install hooks:\nno indicators found",
    "Security scan — obfuscation:\nno indicators found",
    "Security scan — network activity:\nexternal domains referenced: ${rawhost}, api.anthropic.com, api.openai.com, apihub.agnes-ai.com, localhost:${port}, localhost:11434, localhost:3000",
    "Security scan — dangerous file operations:\nno indicators found",
    "Security scan — cron/scheduled tasks:\nno indicators found",
    "Security scan — Dockerfile/container risks:\nno indicators found",
    "Security scan — declared dependencies:\npackage.json: @types/node@^26.1.0, tsx@^4.23.0, typescript@^6.0.3"
  ]
}